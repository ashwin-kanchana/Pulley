# Uncomment the next line to define a global platform for your project
platform :ios, '15.0'

target 'Pulley' do
  use_frameworks!
  pod 'AKNetworkManager', :path => 'AKNetworkManager/'
  pod 'AKUserDefaultsManager', :path => 'AKUserDefaultsManager/'    

end
